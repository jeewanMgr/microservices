package com.jeewan.magar.roomservices.entity;
import javax.persistence.*;

@Entity
@Table(name="TABLE")
public class Room {

	@Id
	@GeneratedValue(generator = GenerationType.IDENTITY)
	@Column(name="ROOM_ID")
	private long id;
	
}
